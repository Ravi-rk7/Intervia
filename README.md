# InterviewIQ

InterviewIQ is a full-stack interview practice application with a React client and an Express API.

## Project structure

- `client` — Vite React application
- `server` — Express API and MongoDB models

## Local development

Install dependencies in both folders, then run the client and server in separate terminals.
